"scripts": {
  "ng": "ng",
  "start": "ng serve",
  "build": "ng build",
  "vercel-build": "ng build --configuration production",
  "watch": "ng build --watch --configuration development",
  "test": "ng test"
}